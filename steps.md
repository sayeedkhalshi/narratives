1. Tested with mongodb and prisma and it doesn't look good
2. Lets learn graph database. So you can use neo4j with graphql, sessions, transactions and more in next.js.
3. Let's check rust support for neo4j. It looks like there's a community-driven project called neo4j-rust that provides a driver for interacting with Neo4j from Rust. GraphQL support can be added using a library like juniper or async-graphql.
4. Rust backend with nextjs frontend and neo4j as the database would be better choice for complex relationships like this
